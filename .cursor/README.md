# Cursor Disabled

이 레포에서는 Cursor Agent를 더 이상 지원하지 않습니다.

- Cursor 쪽 shell 실행은 `.cursor/hooks.json`의 차단 hook으로 막습니다.
- Cursor 규칙은 Claude Code 전환 안내만 남기고 나머지 기능 규칙은 제거합니다.
- 이 레포를 agent로 사용하려면 Claude Code를 이용해주세요.
